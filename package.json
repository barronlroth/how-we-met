{
  "name": "how-we-met",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "python3 -m http.server 4179 --bind 127.0.0.1 --directory dist"
  },
  "dependencies": { "three": "0.185.1" },
  "devDependencies": { "esbuild": "0.28.2" }
}
